# Sway Configuration

Manually copy the example files and customize them:

```bash
cp screen_config_example screen_config
cp terminal_config_example terminal_config
```

- Edit the copied files (e.g., `screen_config`, `terminal_config`) to match your monitor and terminal preferences.
